package org.geepawhill.html.css.enums

enum class PositionTypeEnum(val value: String) {
    static("static"),
    absolute("absolute"),
    fixed("fixed"),
    relative("relative"),
    sticky("sticky")
}
